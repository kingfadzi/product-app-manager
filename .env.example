# Application Branding
REACT_APP_NAME=Lean Control
REACT_APP_DESCRIPTION=Application and Product Management Platform

# API Configuration
# Set to true to use mock endpoints (MSW), false for real backend
REACT_APP_USE_MOCK=true

# Backend API URL (required when REACT_APP_USE_MOCK=false)
# Set to your lean-web backend server address
REACT_APP_API_URL=http://localhost:8000

# Dev server binding (set to 0.0.0.0 for remote access)
HOST=0.0.0.0
